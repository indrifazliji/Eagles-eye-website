// Display a message when a user submits the form
const form = document.querySelector('form');
form.addEventListener('submit', (e) => {
    alert('Thank you for reaching out! We will get back to you shortly.');
});
